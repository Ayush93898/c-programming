#include <stdio.h>
int main()
{
    // int age;
    // float height;
    // char grade;
    // //& symbol is called the "address-of operator." It's used in scanf() to tell the compiler where to store the user's input.
    // printf("Enter your age: ");
    // scanf("%d", &age); // int input

    // printf("Enter your height in meters: ");
    // scanf("%f", &height); // float input

    // printf("Enter your grade: ");
    // scanf(" %c", &grade); // space before %c to ignore leftover newline

    // printf("\nYou entered:\nAge: %d\nHeight: %.2f\nGrade: %c\n", age, height, grade);

    int a = 1;
    int b = 2;
    int sum = a+b;
    printf("%d",sum);
    return 0;
}